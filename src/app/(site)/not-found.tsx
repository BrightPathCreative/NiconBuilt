import { NotFoundContent } from "@/components/NotFoundContent";

/** 404 for site routes — header and footer come from the `(site)` layout. */
export default function NotFound() {
  return <NotFoundContent />;
}
