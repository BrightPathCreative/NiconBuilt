# Nicon Built — environment variables
# Copy to .env.local and fill in real values. Never commit real secrets.

# Public site URL
NEXT_PUBLIC_SITE_URL=https://niconbuilt.com.au

# Google Analytics 4 Measurement ID
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# GoHighLevel (GHL) webhook endpoint — all contact forms POST here, never mailto:
GHL_WEBHOOK_URL=

# GHL lead-gen phone number (public, click-to-call, site-wide) — Sara to provide towards the end of the build
# Do NOT use 0407699454 anywhere — that is Nick's personal mobile, a placeholder only
NEXT_PUBLIC_PRIMARY_PHONE=

# VBA builder's licence number — add when Nick supplies; blank OK until launch
NEXT_PUBLIC_VBA_LICENCE_NUMBER=

# Google Business Profile / Places ID (optional — only if pulling reviews live instead of using the 9 static reviews already approved in docs/copy/testimonials.md)
GOOGLE_BUSINESS_PROFILE_ID=
